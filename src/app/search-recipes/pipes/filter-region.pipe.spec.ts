import { FilterRegionPipe } from './filter-region.pipe';

describe('FilterRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
