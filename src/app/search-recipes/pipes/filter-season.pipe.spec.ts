import { FilterSeasonPipe } from './filter-season.pipe';

describe('FilterSeasonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeasonPipe();
    expect(pipe).toBeTruthy();
  });
});
